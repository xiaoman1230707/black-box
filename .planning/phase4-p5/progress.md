# 第四期 P5 进度

- 2026-07-17：用户确认 P5.1～P5.10 整批人工验收通过，允许进入 P6 方案阶段。P5 规划关闭；未直接执行 P6。
- 2026-07-17：P5.10 完成。聚焦测试先复现 `start:prod=node dist/main` 与 `dist/src/main.js` 的真实产物差异，最小修正 package script 后转绿；build、聚焦 lint 0/0、全量 Jest 14 suites/63 tests 通过。隔离端口 3106 使用标准 `pnpm start:prod` 成功启动，`GET /api` 返回 200/`Hello World!`；仅终止经命令行核对的隔离 PID，测试前 3000 已无监听且未被本 task 终止，最终 3000/3106/5173 均无监听。本 task 未运行 install、未修改既有 lockfile；P5.1～P5.10 已全部闭环，停止在 P6 入口。
- 2026-07-17：用户人工复验 P5.9 通过：Search 慢响应进入可重试 ErrorState，Chat 引用 timeout 后约 23.6 秒开始、43.6 秒完成并恢复输入，数据不变。P5.9 已关闭。进入 P6 前新增唯一 P5.10 门禁：`start:prod` 当前指向不存在的 `dist/main`，实际 build 产物为 `dist/src/main.js`；按 TDD 最小修正并使用隔离端口验证，不触碰现有 3000 进程。
- 2026-07-17：P5.9 TDD 实施完成。后端新增 deadline/env/Embedding/AIService/controller 测试，前端新增 timeout fetch/Search/Axios 错误测试；后端 13 suites/62、前端 11 files/27、双端 build、触及文件 lint 0/0、全量前端批准 3/0、7 files/41 Playwright 全通过。真实 Search 在供应商 embedding 仍慢时于 20.03 秒有限返回 code1；真实 Chat 的引用检索 timeout 后成功降级，DeepSeek 随后完成 HTTP 201 data-stream v1 的 0:/d:，总耗时 37.54 秒。临时 smoke 已删除、服务已停止；等待用户人工复验，不进入 P6。
- 2026-07-17：用户确认分层有限失败方案并授权施工；已写回 04 与实施计划 P5.9。固定 embedding 20s、DeepSeek 30s、Search axios 25s、Chat custom fetch 55s，外部 SDK `maxRetries:0`。Search 后端失败进入既有 ErrorState；Chat 未提交响应返回 504，已提交 data stream 写 `3:` error part；引用检索失败继续降级为空引用。Axios 无 response/config 不进入 401 refresh；精确 CORS 不扩大。下一步先写 RED 测试，再最小实现与全量回归。

- 2026-07-16：用户确认 P4 人工验收通过；foundation、04、实施主计划、P4 QA 与 `.planning/phase4-p4/` 已同步。
- 2026-07-16：活动计划切换为 `phase4-p5`，开始只读代码核对与施工方案编写；本轮不实施 P5、不安装依赖、不写环境值、不运行维护/seed 命令、不提交 Git。
- 2026-07-16：完成前后端 URL、AI SDK、env/JWT/AI 配置、媒体路径、CORS、controller、Prisma、uploads、seed/backfill、依赖和测试基线核对；未读取或记录 `.env` 值。
- 2026-07-16：订正 04 与主计划中的限流身份、环境 profile、cleanup 控制文件、seed 文件补偿、embedding 失败码和差分 lint 契约；P5 八任务方案已完成，等待用户确认后才实施。
- 2026-07-16：自审通过：P5.1～P5.8 依赖顺序、真实文件矩阵、TDD/人工门禁、数据执行门禁和回滚边界闭环；无 TBD/TODO/“后续再定”，文档补丁检查无错误。本轮未修改业务源码、依赖、测试、环境或数据。
- 2026-07-16：评审认可后端差分 lint 与 P5 技术方向；按阻塞意见补齐前端 build/CI 显式 env、Playwright `webServer.env`、后端 config 唯一 localhost 例外和单实例内存限流部署边界，并清除两处 P4 过期状态。本轮仍只修文档，不实施 P5。
- 2026-07-16：用户允许开始施工；按 executing-plans 从 P5.1 开始。确认前端无 `.env/.env.local/.env.example`，硬编码仅在 axios config 与 Chat hook，Playwright webServer 尚无测试 env 注入。
- 2026-07-16：P5.1 runtime 单测取得有效 RED 后实现并 3/3 GREEN；首次“缺 env build”意外成功，证明浏览器模块运行时校验不足以构成构建门禁。先回填计划，增加 `runtime-value.ts` 与 `vite.config.ts` 复用纯校验，随后再实现。
- 2026-07-16：P5.1 完成：缺 env build 非零，显式值/尾斜杠值 build 2455 modules；定向 lint 0/0、全量 3/0、6 files/19 unit、7 files/41 Playwright、前端 src localhost 零命中。首次 e2e 的 39 个页面空白由旧 5173 Vite 被复用导致，停止已核实的本项目旧进程后 41/41 通过。
- 2026-07-16：P5.2 parser 与消费者接线完成阶段验证：env 7/7、后端全量 2 suites/8 tests、build 通过；新增 config 文件 lint 0/0，10 个历史文件逐文件诊断均未高于施工前基线。弱 secret 子进程按规则非零退出且不回显值；真实 `.env` 启动同样因现有 TOKEN_SECRET 不符合新强度规则而被正确阻断。未读取/记录该值，也未擅自修改 `.env`；等待用户决定自行更新或授权生成替换后完成真实启动门禁。
- 2026-07-16：用户授权后，仅替换本机 `.env` 的 TOKEN_SECRET。首个静态 RNG API 在当前 PowerShell/.NET 上不兼容且命令错误后继续执行，随即在任何服务启动前使用兼容的 `RandomNumberGenerator.Create().GetBytes()` 重新覆盖，并以不输出值的长度、随机性特征和禁用片段检查确认；中间值从未用于服务启动。沙箱内首次真实启动因新依赖文件读取 `EPERM` 被 Node 表现为 `MODULE_NOT_FOUND`，核对 junction/目标存在后在获准的非沙箱环境复验：`dotenv` 可解析，后端以真实 `.env` 在 3101 完整启动，`GET /api` 返回 200/`Hello World!`，随后进程停止且端口无残留。P5.2 门禁完成。
- 2026-07-16：P5.3 按 TDD 完成 `public-url` helper（RED 为缺模块，GREEN 9/9），五个 posts/auth/comments/ai/upload service 只替换 URL 构造并保留空值、字段和磁盘语义。新增文件 lint 0/0，AI/Auth 回到既有 183/0、80/2，其他历史文件未新增诊断；build 与后端全量 3 suites/17 tests 通过，五个消费者 localhost/`BASE_URL` 零命中。显式测试域名下真实后端只读验证 14 帖、详情/评论/登录均成功；当前数据无头像/帖子图片，故没有非空媒体 URL 可作接口样本，未调用上传写接口也未伪报该项。验证后 3102 进程与端口已清理。
- 2026-07-16：P5.4 增加纯 `cors-options` 契约并 TDD。首版使用字符串 origin 时，真实恶意相似 origin 预检仍收到“配置 origin”响应头（浏览器虽会因不匹配阻断，但不符合无头契约）；增强 RED 后改为精确 callback，4/4 GREEN。最终允许 origin 204、Authorization/Content-Type 与 GET/POST/DELETE/OPTIONS 正确，无 credentials；相似 origin 404 且无 CORS origin 头。env 新增 exact origin/loopback 测试，后端全量 4 suites/22 tests、build 通过；新增文件 lint 0/0，main.ts 从基线 13/1 降至 11/1。3103 服务与端口已清理。
- 2026-07-16：P5.5 安装官方 `@nestjs/throttler@6.5.0`，按本地 v6 真实扩展点 TDD 完成全局 guard、对象式 route 配额、显式 user-or-IP metadata 与结构化 429；后端限流测试 12/12，后端全量 6 suites/34 tests。Chat `onResponse` 429 接缝 2/2，前端全量 7 files/21 unit、build 2455 modules、定向 lint 0/0、全量批准基线 3/0、Playwright 7 files/41 passed。低限额真实验证：login 200/200/429 且第三次带 token 仍按 IP；search 同用户 200/429、另一用户 200；upload 同用户 400/429、另一用户 400且无写入；匿名 chat 401；限额内授权 chat 201 并保留 `x-vercel-ai-data-stream:v1`、text/finish parts。首次 PowerShell curl JSON 参数被拆坏导致 400，改用 UTF-8 临时文件后完成有效请求复验；3104 已清理。新增后端文件均 lint 0/0，五个历史文件的新增行归零并使诊断降至 AI 71/0、AppModule 1/0、Auth 8/0、Upload 23/2、Users 8/0。
- 2026-07-16：P5.6 planner/executor TDD 6/6，覆盖 dry-run、严格 24h 边界、unsafe DB filename、Windows junction no-follow、缺派生、apply 双确认和单文件失败继续/非零；新增 maintenance/CLI 文件 lint 0/0、build 通过。真实无参数 dry-run exit 0：control 4、missing 0、orphan 40、protected 0、referenced 2、symlink/unknown/unsafe 0；前后 uploads 文件 SHA-256/mtime/size 完全一致，DB Avatar/File 计数保持 1/2。首次 PowerShell 包装把 pnpm 横幅 stderr 当终止错误，改为按真实退出码重跑。未执行 `--apply`，40 个候选及两条孤立 File 记录均未删除；apply 继续保留独立人工授权门禁。
- 2026-07-16：P5.7 按 TDD 完成 35 帖 manifest 与图片文件补偿边界，8/8；固定 5 名作者、5 游戏各 7 帖、每游戏五内容类型、保留三期 14 标题、10 张独立生成的 1600×900 JPEG，并锁定评论 parent、点赞去重和无评论时间。seed 不再按作者删全部帖子，改为“作者+标题”定向删帖和“演示作者+fixture originalname”定向删 File；图片已有则复用不覆盖，本次新建路径才加入补偿，数据库 interactive transaction 失败后逐项补偿且残留时报非零。backfill 任一失败最终非零，package 增加三个组合命令。新增/修改脚本 lint 0/0、build 通过、后端全量 9 suites/48 tests；沙箱内测试曾因 pnpm junction 无法解析 throttler 失败，获准环境全量复验通过。未执行真实 seed、full 或 embedding 回填，数据写入与外部成本门禁仍待单独授权。
- 2026-07-16：P5.8 新增后端 `.env.example`、部署与维护文档，完善前端示例，明确显式 build/CI env、Playwright 固定值、runtime/维护 profile、精确 CORS、loopback proxy、单实例内存限流、强 secret、备份恢复、cleanup 与 seed/full 门禁。静态 localhost 门禁订正为排除 co-located 测试后扫描：前端业务源码零命中、后端仅集中 env.ts 两处开发 fallback。前端显式 env build 2455 modules、批准 lint 3/0、7 files/21 unit、7 files/41 Playwright；后端 build、9 suites/48 tests。沙箱内 build/unit/e2e 均因 pnpm junction EPERM 失败，获准环境复验全部通过；失败运行产生的测试进程按 PID 核对后清理，最终 3000/3101-3104/5173 无监听。P5 工程实现完成，但 cleanup apply 与真实 seed/full 两次幂等仍待用户独立授权，未进入 P6。
- 2026-07-16：P5.6 评审指出 File.filename 非唯一而 planner 单记录 Map 会让同名引用状态后写覆盖。新增“已引用+孤立同名”“多个孤立同名”两项 RED，分别复现误计划删除与只删末条记录；改为按 filename 聚合全部 IDs 和 any-referenced 后 8/8 GREEN，完整删图后才逐条删全部孤立记录。定向 lint 0/0、build、全量 9 suites/50 tests。修复后真实 dry-run 明细为 18 头像组+2 帖子图组，共 40 候选文件；1 个引用头像组 2 文件保留；当前 Avatar 1、File 2、重复 filename 0。46 个 uploads 文件 dry-run 前后 SHA-256/mtime/size 差异 0。用户确认 yue 为演示库并接受两轮 embedding 成本；本机 DEMO_USER_PASSWORD 已随机配置且未回显，首次不兼容 ToHexString 曾短暂写空，任何服务/seed 运行前已用兼容 BitConverter 覆盖并验证长度 48。真实 apply/seed/full 仍因备份文件与 SHA-256 未闭环而未执行。
- 2026-07-16：用户验收 P5.6 修复并允许备份 A。3000/5173 无监听的停写窗口内，在仓库外 `C:\Users\15593\Black-box-backups\backup-A-20260716-171200` 生成 yue custom dump 与 uploads tar.gz；大小分别 133256/645428 bytes，SHA-256 分别为 `6BC32045A57F7BCA7B94E73D852BF3E32FA4457A6B85B99EFC614D1CB7E1022C`、`E768D1EBB57C5248D581348145E098AA704DDC18BE96F8BB8EE879B0D31CD7CA`。`pg_restore --list` 98 条、`tar -tzf` 50 条，完成后独立重算一致；证据写入 `docs/qa/phase4/p5-backup-a.md`。未配置隔离恢复目标，未做副本 restore；cleanup apply、seed/full 均未执行，等待下一次单独授权。
- 2026-07-16：用户仅授权命令 `pnpm maintenance:uploads -- --apply --backup-confirmed --protect-hours=24` 与当前候选集；命令单次执行 exit 0、无 FAILED。20 组/40 文件删除，File 4/5 删除；后续 dry-run orphan 0、referenced 2、control 4，其余 0。只读核对 Post 14、Avatar 1、File 0、File 4/5 命中 0；uploads 恰余 4 控制文件与引用头像 large/small 共 6 文件。备份 A 两份哈希复核不变，3000/5173 无监听。证据写入 `docs/qa/phase4/p5-cleanup-apply.md`；未执行 seed/backfill 或创建备份 B，等待 cleanup 人工验收。
- 2026-07-16：cleanup apply 交付前完成最终只读复核。沙箱内 `pnpm maintenance:uploads` 因已登记的 pnpm junction 依赖解析限制报 `dotenv` TS2307，获准环境重跑 exit 0，仍为 control 4、orphan 0、referenced 2、其余 0。首次内联 `ts-node` DB 计数被 PowerShell 引号破坏，未连接数据库；改用 CommonJS 查询后虽取得正确计数但 `$disconnect` 被 shell 展开导致 exit 1，随后以不含 `$` 字面量的等价调用复验 exit 0：Post 14、Avatar 1、File 0、File 4/5 命中 0。`git diff --check` 无补丁错误（仅既有 LF/CRLF 提示），uploads 仍为精确 6 文件，备份 A 两份 SHA-256 不变，3000/5173 监听 0。未创建备份 B、未执行 seed/backfill、未进入 P6。
- 2026-07-16：用户确认 cleanup 人工验收通过并允许创建备份 B。停写状态下在仓库外 `C:\Users\15593\Black-box-backups\backup-B-20260716-174727` 生成 cleanup 后、seed 前恢复点：Post 14、Avatar 1、File 0，uploads 6 文件。数据库 custom dump 133135 bytes，SHA-256 `F901DA0A5552DF1D29423AA821BD0B7DB1A8487AE85E217BBEB54A9AE5F3CB3A`；uploads tar.gz 27764 bytes，SHA-256 `D6EC377E2B63F02C25090AC8C7A9D02D88BD15FC41FD5F8CC01BD3FF2B56F7B3`。`pg_restore --list` 98 行/83 个非注释项，`tar -tzf` 10 项，独立复算一致。首次 pg_dump 因 Prisma URL 的 `schema` 参数被 PostgreSQL 18 拒绝，在访问数据库前失败；重试仅从内存连接副本移除该参数，不改 `.env`，0 字节失败目录已核验删除。证据见 `docs/qa/phase4/p5-backup-b.md`；未执行 seed/full、embedding 或 P6，等待独立授权。
- 2026-07-16：用户明确授权在 `yue` 演示库连续执行两次 `pnpm seed:demo:full`。两轮均单独执行并 exit 0：第一轮定向替换 14 帖/0 File，第二轮定向替换 35 帖/10 File；两轮最终均为 35 帖、13 评论、31 点赞、10 图，各 35 个 `text-embedding-3-small` embedding 全部成功，共 70 次外部调用。最终 5 游戏各 7 帖且每游戏覆盖五种类型，正文空 0、重复标题 0、35/35 向量维度 1536、File 全关联且重复 filename 0。两轮间 10 张缩略图哈希逐一不变；uploads 共 26 文件，cleanup dry-run control 4、referenced 22、orphan 0，其余 0。备份 B 哈希未变，3000/5173 无监听。证据见 `docs/qa/phase4/p5-seed-full.md`；未执行第三轮或额外 Search/Chat AI 调用，P5 等待页面人工串验，未进入 P6。
- 2026-07-17：用户人工串验判定 P5 部分通过：Home 与 PostDetail、数据库终态、cleanup 和备份 B 均通过；Search 超过 50 秒仍搜索中，Chat 超过 30 秒无 SSE 内容。系统化诊断确认两路共用 `retrieveTopK → EmbeddingService` 前置链路且前后端均无截止时间；本地 SDK 类型显示底层 OpenAI 客户端默认超时 10 分钟并默认重试。受控最小实测为 embedding 200/1536 维/11766ms，DeepSeek chat 首字节 200/395ms，说明当前供应商已恢复但 embedding 明显较慢，既有长期 loading 可由共享 embedding 的临时迟滞叠加默认长超时解释。另确认 Axios 拦截器直接读取可空 `response.status`，无响应错误会二次抛错。两次只读 `rg` 探索分别遇到 Windows wildcard 与 PowerShell 引号错误，改为先解析真实 pnpm 目录和简单 pattern 后完成核对；未修改生产代码。下一步待用户确认分层有限截止时间方案后按 TDD 实施，P5 不进入 P6。
