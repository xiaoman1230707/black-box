# D3.6 SSH/SFTP/SCP 只读差异诊断

> 日期：2026-07-20
> 状态：只读诊断完成；未上传、未创建 reboot marker、未重启、未修改远端配置
> 原始边界：调试日志仅保存于仓库外临时目录，已生成脱敏副本并删除 raw 日志

## 1. 结果矩阵

| 测试 | 结果 | 失败/成功阶段 |
|---|---|---|
| 普通 SSH `printf` 基线 | 失败 | TCP 连接建立后、认证完成前由对端关闭 |
| SFTP `pwd/ls/quit` | 成功 | 公钥认证、SFTP subsystem、命令执行和 exit status 0 全部完成 |
| 默认 SCP 只读下载 `/etc/os-release` | 成功 | 使用默认 SFTP 协议完成；本地文件 SHA-256=`594D5DDD35AEDB47F00D9C34D140017907A5B9F93C975ABA125FC924DAAC5C07` |
| legacy SCP `-O` 同文件下载 | 失败 | TCP 连接建立后、认证完成前由对端关闭；未生成本地文件 |
| SSH journal 只读采集 | 跳过 | 新连接在认证前关闭，未进入 `sudo -n` 判断；未采集密码 |

## 2. 判断

- SFTP subsystem 与默认 SCP 的 SFTP 数据路径均已真实成功，不能将此前上传失败归因于 SFTP subsystem 缺失或服务端禁用。
- 失败会话均发生在认证前，尚未到达远端目标路径检查或文件写入阶段，因此没有证据指向 `/home/deploy` 权限或上传目标路径。
- 同一诊断窗口内普通 SSH、legacy SCP 和 journal 新连接也出现认证前关闭，而 SFTP/default SCP成功，当前证据更符合短时间多连接或本地网络路径的间歇性连接异常。
- 不修改 `sshd Subsystem`、UFW、安全组、DIRECT、密钥、用户权限或软件包。正式上传仍须单独恢复授权，不因本次只读成功自动重试。

## 3. 证据卫生

- 仓库外临时目录：系统临时目录下 `black-box-d3-transfer-diagnostic-20260720T170253`。
- 调试日志已将公网地址和用户目录/密钥路径替换为占位符；raw 日志数量为 0。
- 本轮遗留的本地 `ssh/scp/sftp` 客户端进程已清零。
- 未记录真实管理源地址、ECS公网地址、私钥路径、密码或服务 secret。

## 4. 当前门禁

D3.6 仍停在修正版预检脚本正式上传之前。未创建 reboot marker，未执行 `systemctl reboot`，不得把本次诊断视为上传或重启授权的自动延续。

## 5. 诊断后的单次上传恢复结果

- 用户另行批准使用默认SFTP和唯一版本名恢复一次；批处理契约为上传到`.part`、设置`0700`、同一会话rename到最终路径。
- 该连接在认证完成前被关闭，本地SFTP输出为空；已按约束停止且未自动重试、未切换legacy SCP、未建立后续SSH核验连接。
- 本轮没有执行脚本、没有创建reboot marker、没有执行重启。本地传输客户端进程已清零。
- 由于没有后续远端只读核验，不声明`.part`存在或不存在；后续必须先检查唯一目标现场再决定处置。

## 6. 唯一目标现场只读核验

- 用户单独批准一次普通SSH，仅核对三个精确路径；未使用SFTP/SCP、sudo、通配符、枚举或写操作。
- 本次唯一`.part`路径：不存在。
- 本次唯一最终路径：不存在。
- 旧的不确定路径：存在，`deploy:deploy 0700`、2307 bytes、SHA-256=`E23459B8298610D4494EA310CC0BF175D36D966F9CD4B8DFE5961BA8B1270372`。
- 结论：本次唯一SFTP在远端落盘前失败；旧路径仍是旧版本，与当前本地修正版SHA不一致，继续禁止执行。未创建marker，未重启。

## 7. SSH stdin 单次传输失败现场

- 用户另行授权一次Base64/二进制安全的单SSH stdin传输，目标为新的唯一路径；旧路径未覆盖、未删除、未执行。
- SSH进程退出码为0且stdout曾输出`REMOTE_SHA_MATCH=true`，但仓库外stderr复核发现`base64: invalid input`、`cut`分隔符解析失败和`stat`格式参数拆分。
- 根因是远端命令经本地OpenSSH参数重组后丢失必要引号，且命令未使用可靠的fail-fast封装；SHA条件出现shell表达式错误后继续执行，故stdout成功标记属于控制流误报，禁止采信。
- 唯一最终路径`/home/deploy/d3-reboot-precheck-apply-20260720T093110Z-b7185a06e83f-stdin.sh`已生成，但真实SHA、group/mode/size尚未可靠核验；只确认owner输出为deploy，`bash -n`返回成功也不足以证明内容完整。
- 按失败即停约束，未建立第二连接、未执行该文件、未创建marker、未reboot、未清理失败制品。本地传输进程为0。

## 8. 失败制品独立只读核验

- 用户另行批准一次普通SSH只读核验；仅检查上述唯一最终路径，未读取正文、未修改、未运行脚本业务逻辑、未使用sudo。
- 终态元数据为`deploy:deploy 0700`、2361 bytes，远端SHA-256=`B7185A06E83F406F5FF0CED9966FA31291D3965DDB11555BDF0D485A62E047A5`，与本地修正版精确一致；`bash -n`通过。
- 这证明Base64 decoder在报告尾随无效输入前已完整写出有效载荷。此前stdout成功标记仍不能作为证据；本次独立只读SHA/元数据/语法核验才构成制品完整性证据。
- 该文件现在可作为后续独立门禁的候选脚本，但本次授权不包括执行它、创建marker或reboot。

## 9. Reboot marker 门禁未完成

- 用户随后独立授权执行该唯一候选脚本并创建marker，但明确不授权reboot。
- 单SSH wrapper先再次核对候选SHA并验证`sudo -n true`，两项均成功；调用候选后，连接只返回前置标记，未返回预检脚本完成标记、wrapper退出码、marker或证据摘要。
- 因完整成功契约未满足，门禁判定失败并立即停止；未重试、未建立第二连接、未执行reboot。
- 当前不声明marker或新证据目录存在/不存在；下一步必须通过单独授权的精确只读检查定位失败现场。

## 10. Marker 精确只读现场检查

- 用户批准一次普通SSH，只检查固定marker；仅在marker存在时才允许跟随其中登记的证据目录，本次未使用枚举或通配符。
- 结果为`MARKER_EXISTS=false`，说明候选预检脚本在写marker之前失败，D3.6 marker门禁未完成。
- 因marker不存在，没有受控的证据目录路径可继续核对；未枚举`/root`、未修改或清理现场、未重试、未reboot。
