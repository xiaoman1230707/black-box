# O1.1 Topbar 物理删除与 Chat 高度验收记录

> 日期：2026-07-17  
> 状态：实现与自动验证完成，等待用户人工确认

## 修改边界

- `MainLayout.tsx`：仅删除 Topbar import 和 render，保留 Sidebar、Shell grid、内容容器、移动底栏 padding 与 Outlet。
- `Topbar.tsx`：确认除自身定义外零引用后物理删除；删除后 `frontend/black_box/src` 内 `Topbar` 零命中。
- `Chat.tsx`：只把桌面和移动高度公式中的旧 Topbar `4.5rem` 扣减移除，`7.5rem` 收口为 `3rem`；消息、SSE、JWT、store、annotation 和纯文本呈现均未改变。

## 自动验证

| 检查 | 结果 |
|---|---|
| MainLayout + Chat 定向 lint | 0 errors / 0 warnings |
| 前端 build | 成功，2455 modules transformed |
| Home + auth-guard + ai-chat | 18 passed |
| Topbar 引用扫描 | 0 命中 |
| 截图 | `docs/qa/phase4/screenshots/o1-1/`，28/28 |

## 四视口检查

一次性 QA 检查覆盖 1440×1000、900×1000、390×844、320×740：

- Home：每档 `searchCount=1`。
- Search：每档 `searchCount=1`。
- Chat、Compose、Mine：每档 `searchCount=0`。
- 上述页面每档均满足 `documentElement.scrollWidth === clientWidth`。
- Chat 输入栏在移动底部导航上方，未被遮挡；消息 Card 只保留内部滚动区，没有新增页面双滚动。
- Login 仍使用独立布局。

首次 320px Search 截图在 HMR 切换时捕获到不完整按钮画面；DOM 实测页面宽度 320/320、SearchBar 范围 72～304、提交按钮范围 260～304（44px），确认不是布局溢出。HMR 稳定后覆盖重拍，同一 stage 截图已完整显示按钮。

## 范围保护

- 未修改 Home、Search、SearchBar、Sidebar、router、`useChatBot`、`useChatStore`、Markdown 安全策略或后端。
- 未接入 MarkdownRenderer，未新增或修改 Playwright e2e，未提交 Git。
- O1.2 在用户确认 O1.1 前保持未开始。
