# O1.3 Chat assistant Markdown 接线验收记录

> 日期：2026-07-17  
> 状态：TDD、实现与聚焦回归完成，等待用户人工确认

## 实现边界

- Chat 新增唯一 `MarkdownRenderer` import。
- `message.role === "assistant"` 时使用 `variant="chat"`；其他角色继续使用 `whitespace-pre-wrap` 纯文本。
- citation 读取、过滤、`/post/:id` Link chip、testid 与“正文后呈现”的 DOM 顺序保持。
- 消息 key、气泡、avatar、loading/error、form/input 和 submit 行为均未修改。
- `useChatBot`、`useChatStore`、JWT、SSE、timeout、429 与后端零改动。

## TDD 证据

### RED

- 新增 `Chat.test.tsx`，使用 mocked `useChatbot` + `MemoryRouter` SSR。
- 首次运行：assistant Markdown 测试失败，user 纯文本测试通过。
- 失败原因是旧实现没有 `data-variant="chat"`、h2/list/strong，符合目标。

### GREEN

| 检查 | 结果 |
|---|---|
| Chat 角色测试 | 2 passed |
| Chat + renderer + hook 聚焦 unit | 3 files / 16 passed |
| Chat + Chat test 定向 lint | 0 errors / 0 warnings |
| 前端 build | 成功，2455 modules transformed |
| 既有 `ai-chat.spec.ts` | 3 passed |
| protected SHA-256 临时复核 | 0 差异 |

## 契约验证

- assistant Markdown 输出 h2、列表与强调语义，并标记 `data-variant="chat"`。
- user 内容 `**用户原文** [链接](/post/1)` 保留源字符，不生成 strong 或 Link。
- citation 仍指向 `/post/39`，并位于 assistant Markdown 正文之后。
- 既有 `chat-message`、`chat-citations`、`chat-citation-link` 均保留。
- 既有 Playwright 继续锁定流式响应、引用可点和切页会话保持；没有修改其断言或数量。

## 门禁结论

- 本阶段只完成呈现接线，没有改变 AI 数据流或协议。
- 未新增 O1 Playwright e2e；专项流式 Markdown、多视口和真实链路留在 O1.4 人工门禁。
- 未提交 Git。
- O1.4 在用户确认 O1.3 前保持未开始。
