# F6.2 Docker容器漂移只读诊断停止报告

> 状态：首次只读采集因Docker模板函数不兼容而停止；远端状态未改变

## 1. 已取得事实

- SSH与`sudo -n`只读门禁成功。
- `docker ps -aq --no-trunc`确认当前共有`2`个容器，确实偏离F6.1“仅生产db一个容器”的终态。
- 在输出第一个容器详情前，远端Docker模板解析器拒绝未提供的`trimPrefix`函数，命令以非零状态退出。

因此当前只能确认容器总数为2；尚不能判断第二个容器的名称、Compose标签、状态、创建时间或来源，也不能据此决定清理或放宽审计口径。

## 2. 未发生事项

- 未执行Docker或Compose写命令；
- 未启动、停止、删除、重启或清理任何容器；
- 未读取数据库或secret；
- 未创建远端目录或文件，未上传、导入镜像或恢复F6.2；
- 未修改Docker、Nginx、UFW、安全组、SSH或其他主机配置。

## 3. 本地修正与恢复边界

本地诊断脚本只将不兼容的`trimPrefix`替换为Docker基础字段`.Name`；其余只读字段、events和daemon journal范围不变，并已保留首次失败原始输出。按用户要求未自动重连或重试。需要新的单次只读诊断授权后，才能取得两个容器的完整身份和时间线。
