# Required at build/dev time. Copy to an untracked .env.local for local development.
# CI must inject this variable explicitly; Vite does not load .env.example.
# Playwright supplies its own fixed test value through playwright.config.ts.
VITE_API_BASE_URL=http://localhost:3000/api
